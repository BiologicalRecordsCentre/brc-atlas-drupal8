name: BRC Atlas library interface for Drupal
description: Implements the BRC Atlas library within a Drupal module.
package: Custom

type: module
core_version_requirement: ^8.8 || ^9
configure: brc_atlas.config